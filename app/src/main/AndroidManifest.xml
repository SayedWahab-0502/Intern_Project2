<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.game_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Game_Completion"></activity>
        <activity android:name=".Camera_Open22" />
        <activity android:name=".Camera_Open11" />
        <activity android:name=".User_Found111" />
        <activity android:name=".User_found" />
        <activity android:name=".Find_random_users" />
        <activity android:name=".Login_Screen" />
        <activity android:name=".Register_screen" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.TransparentTheme" />
        <activity android:name=".Splash_Screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>